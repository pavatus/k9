package mdt.falloutmod.core.blocks;

import mdt.falloutmod.core.blocks.types.HorizontalDirectionalBlock;

public class RadioBlock extends HorizontalDirectionalBlock {
    public RadioBlock(Settings settings) {
        super(settings);
    }
}
